module.exports = {
  name : 'user',
  aliases : ['whois', 'userinfo'],
  description : 'user details.',
  usage : '[optional : <mention> or <id>]',
  execute({msg,args}){
    let user = {};
    if(args[0]){
      let id = args[0]
        .replace()
    }
  }
}