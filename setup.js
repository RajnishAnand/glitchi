function log(i, t) { console.log('\033[' + i + 'm' + t + '\x1b[0m') } log(34, 'checking for required TOKENs...');if(!process.env.TOKEN)try{require('dotenv').config()}catch{};let hasKey=true;for (let key of ['TOKEN','ACOKEY','FIRE','API0','UNSPLASH'])if(!(key in process.env)){log(33,`'${key}' not Found!`);hasKey=false;};if(!hasKey){log(31, 'Please declare these keys in .env file at the root of your directory or in SECRET');return;}global.config = require('./config.json');log(32, 'Success !');config.prefix = config.prefix[+config.beta];log(34, 'logging in...');require('./build/index.js');if (config.HTTPserver) require('./server.js');process.on('unhandledRejection', console.log);