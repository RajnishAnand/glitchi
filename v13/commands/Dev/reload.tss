import {Command} from "Interfaces";

export const command : Command = {
  name : "reload",
  description : "Reload commands",
  devOnly : true,

  async run({msg}){
    msg.client.init();
    msg.reply("All commands refreshed.")
  }
}
